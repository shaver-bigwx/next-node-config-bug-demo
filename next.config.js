const config = require('config');

const { publicRuntimeConfig } = config

const nextConfig = {
  /* comment out the following line for next to start */
  publicRuntimeConfig
};

module.exports = nextConfig
